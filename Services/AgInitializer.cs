using Core.Database.Entities;
using SQLiteRepository;
using SQLiteRepository.Providers;

namespace Services
{
    public class AgDatabaseService
    {
        //public void RegisterProviders()
        //{
        //    ProvidersManager manager = new ProvidersManager();
        //    //Регистрируем провайдеры
        //    manager
        //        .Register<EmployeeEntity>(new EmployeeProvider())
        //        .Register<EstablishmentEntity>(new EstablishmentProvider())
        //        .Register<DepartmentEntity>(new DepartmentProvider())
        //        .Register<FunctionEntity>(new FunctionProvider())
        //        .Register<TimeIntervalEntity>(new TimeIntervalProvider())
        //        .Register<TimeIntervalTypeEntity>(new TimeIntervalTypesProvider());
        //}
    }
}
