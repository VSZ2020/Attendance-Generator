<UserControl x:Class="AG.UserControls.ValidationMessagesList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AG.UserControls"
             xmlns:vm="clr-namespace:AG.WPF.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <ListBox
        Margin="0,5,0,5"
        BorderBrush="Red"
        ItemsSource="{Binding ValidationMessages}" 
        Visibility="{Binding IsValid, Converter={StaticResource invVisibilityConverter}}">
        <ListBox.ItemTemplate>
            <DataTemplate>
                <StackPanel Margin="10,0,10,0">
                    <TextBlock Text="{Binding Title}" FontWeight="Bold" Margin="0,0,0,3" TextWrapping="Wrap"/>
                    <TextBlock Text="{Binding Message}" Margin="0,0,0,3" TextWrapping="WrapWithOverflow" Foreground="Red"/>
                </StackPanel>
            </DataTemplate>
        </ListBox.ItemTemplate>
    </ListBox>
</UserControl>
